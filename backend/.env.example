# PostgreSQL connection (pick ONE style)

# Option A (recommended): single URL
# DATABASE_URL=postgres://postgres:password@localhost:5432/fruits_vault


# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=password
# PGDATABASE=fruits_vault

# Server
PORT=5000

# Auth
# IMPORTANT: set a strong value in production.
JWT_SECRET=change_me
# JWT_EXPIRES_IN=7d
